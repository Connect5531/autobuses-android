<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".vista.ActivityLineas">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Línea"
        android:id="@+id/textView"
        android:layout_marginTop="54dp"
        android:layout_alignParentTop="true"
        android:layout_toStartOf="@+id/textoLínea"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Crear línea"
        android:id="@+id/crearLínea"
        android:layout_below="@+id/textoLínea"
        android:layout_centerHorizontal="true"
        android:onClick="crearLínea"/>

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/textoLínea"
        android:inputType="number"
        android:minWidth="48dp"
        android:layout_marginLeft="24dp"
        android:layout_alignBottom="@+id/textView"
        android:layout_alignEnd="@+id/crearLínea"
        android:maxWidth="48dp"/>

</RelativeLayout>
